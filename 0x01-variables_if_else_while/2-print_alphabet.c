#include <stdio.h>
/**
* main- A program that prints a line with putchar function
*
* Return: Always 0 (success)
*/
int main(void)
{
char i;

for (i = 'a' ; 1 <= 'z' ; i++)
putchar(i);
putchar('\n');
return (0);
}
